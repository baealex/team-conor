pre-commit:
  commands:
    biome:
      glob: "src/**/*.ts"
      run: npx biome check --write {staged_files} && git add {staged_files}
    typecheck:
      run: npx tsc --noEmit

pre-push:
  commands:
    test:
      run: npm test
    build:
      run: npm run build
